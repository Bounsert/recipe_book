# recipe_book
This is my "Recipe Book" website, built from scratch! It started as a simple page and has now grown into a full application where you can register, browse recipes, and much more.  It runs on Python (using the Flask framework) and uses an SQLite database to save everything.
